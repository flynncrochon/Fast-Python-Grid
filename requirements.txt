# fastpygrid runtime dependencies: none. The Tk host is standard library only
# (tkinter) and the renderer DLLs ship prebuilt in the repo.
# The optional Qt host (fastpygrid.render.gpu_qt) needs:  pip install PySide6
