"""Toolkit host adapters for the OpenGL engine (fastpygrid.core.gpu).

Import the host you need directly so an unused toolkit is never imported:
    from fastpygrid.render.tk import make_sheet    # tkinter (stdlib)
    from fastpygrid.render.qt import make_sheet    # PySide6
"""
